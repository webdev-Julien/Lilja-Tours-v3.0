---
import "../../src/styles/global.css"
import HomeHeader from "../../src/components/HomeHeader.astro"
import ItineraryCard from "../components/ItineraryCard.astro"
import { getCollection } from 'astro:content';
import CTAButton from "../components/CTAButton.astro";

// Get all tours and select 4 random ones
const allTours = await getCollection('tours_EN');

// Shuffle array and take first 4
function shuffleArray(array: any[]) {
  const shuffled = [...array];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled;
}

const randomTours = shuffleArray(allTours).slice(0, 4);
---

<html lang="en">
	<head>
		<meta charset="utf-8" />

		<link rel="preload" as="image" href="/videos/hero-mobile-optimized.webp" media="(max-width: 767px)" />
		<link rel="preload" as="image" href="/videos/hero-optimized.webp" media="(min-width: 768px)" />

		    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Discover Iceland with Lilja Tours - private day tours and customized multi-day adventures. Personalized experiences across Iceland's stunning landscapes.">
   <link rel="alternate" hreflang="en" href="https://www.lilja-tours.com/" />
   <link rel="alternate" hreflang="fr" href="https://www.lilja-tours.com/fr/" />
   <link rel="canonical" href="https://www.lilja-tours.com/" />



    <meta property="og:title" content="Lilja Tours - Agence de voyage islandaise">
    <meta property="og:type" content="website">
    <meta property="og:image" content="https://www.lilja-tours.com/videos/hero-optimized.webp">
    <meta property="og:url" content="https://www.lilja-tours.com/">
    <meta property="og:description" content="Discover Iceland with Lilja Tours - private day tours and customized multi-day adventures. Personalized experiences across Iceland's stunning landscapes.">

    <meta property="og:locale" content="en_US">
    <meta property="og:locale:alternate" content="fr_FR">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:logo" content="https://www.lilja-tours/Lilja-Tours-Logo.jpg" />

		<link rel="icon" href="/favicon.ico" type="image/x-icon">
		<meta name="generator" content={Astro.generator} />
		<title>Private tours in Iceland, customization and luxury | Lilja Tours</title>

		<style is:inline>
  /* Critical styles for immediate render */
  .hero-header { 
    position: relative; 
    width: 100%; 
    height: 100vh; 
    overflow: hidden; 
    display: flex; 
    align-items: center; 
    justify-content: center; 
  }
  .hero-image { 
    position: absolute; 
    top: 0; 
    left: 0; 
    width: 100%; 
    height: 100%; 
    z-index: 1; 
  }
  .hero-fallback-image { 
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
    object-position: center; 
  }
  .video-overlay { 
    position: absolute; 
    top: 0; 
    left: 0; 
    width: 100%; 
    height: 100%; 
    background: rgba(0, 0, 0, 0.4); 
    z-index: 2; 
  }
  .hero-content { 
    position: relative; 
    z-index: 3; 
    text-align: center; 
    color: white; 
  }

  /* About Section */
  .about-section {
    background: #fafafa;
    padding: 8rem 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .about-container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .about-text {
    text-align: left;
  }

  .about-title {
    font-size: clamp(2.2rem, 4vw, 3.5rem);
    font-weight: 300;
    letter-spacing: 1px;
    margin: 0 0 2.5rem 0;
    line-height: 1.2;
    color: #1a1a1a;
  }

  .about-paragraph {
    font-size: clamp(1rem, 2.2vw, 1.2rem);
    font-weight: 300;
    line-height: 1.8;
    color: black;
    margin: 0 0 1.5rem 0;
    letter-spacing: 0.2px;
  }

  .about-paragraph:last-child {
    margin-bottom: 0;
  }

  .about-image {
    position: relative;
  }

  .about-img {
    width: 100%;
    height: 500px;
    object-fit: cover;
    object-position: center;
    border-radius: 2px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
  }

  .about-img:hover {
    transform: scale(1.02);
  }

  /* Itinerary Section */
  .itinerary-section {
    background: white;
    padding: 8rem 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .itinerary-container {
    max-width: 1400px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .itinerary-header {
    text-align: center;
    margin-bottom: 6rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .itinerary-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 300;
    letter-spacing: 2px;
    margin: 0 0 2rem 0;
    line-height: 1.2;
    color: #1a1a1a;
    position: relative;
  }


  .itinerary-description {
    font-size: clamp(1.1rem, 2.5vw, 1.3rem);
    font-weight: 300;
    line-height: 1.7;
    color: #666;
    margin: 0 0 3rem 0;
    letter-spacing: 0.3px;
  }

  .itinerary-cta-button {
    background: transparent;
    color: var(--secondary);
    border: none;
    padding: 0.75rem 0;
    font-weight: 300;
    font-size: 1rem;
    letter-spacing: 1px;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    position: relative;
    outline: none;
  }

  .itinerary-cta-button::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 1px;
    background: var(--secondary);
    transition: width 0.3s ease;
  }

  .itinerary-cta-button:hover::after {
    width: 100%;
  }

  .itinerary-cta-button:hover {
    transform: translateY(-2px);
    color: #1a1a1a;
  }

  .itinerary-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2.5rem;
    margin-top: 4rem;
    align-items: start;
  }

  .itinerary-footer {
    display: flex;
    justify-content: center;
    margin-top: 5rem;
  }

  .itinerary-footer .itinerary-cta-button {
    transform: scale(1.15);
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .itinerary-container {
      padding: 0 1.5rem;
    }
    
    .itinerary-grid {
      gap: 2rem;
    }
  }

  @media (max-width: 968px) {
    .itinerary-section {
      padding: 6rem 0;
    }
    
    .itinerary-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
    
    .itinerary-header {
      margin-bottom: 4rem;
    }
  }

  @media (max-width: 768px) {
    .itinerary-section {
      padding: 4rem 0;
    }
    
    .itinerary-container {
      padding: 0 1rem;
    }
    
    .itinerary-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      margin-top: 3rem;
    }
    
    .itinerary-header {
      margin-bottom: 3rem;
    }
    
    .itinerary-title {
      margin-bottom: 1.5rem;
    }
    
    .itinerary-description {
      margin-bottom: 2rem;
    }
  }

  @media (max-width: 480px) {
    .itinerary-section {
      padding: 3rem 0;
    }
    
    .itinerary-grid {
      gap: 1rem;
      margin-top: 2rem;
    }
    
    .itinerary-header {
      margin-bottom: 2rem;
    }
  }

  /* About Section Mobile Responsive */
  @media (max-width: 968px) {
    .about-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .about-text {
      order: 1;
    }

    .about-image {
      order: 2;
    }

    .about-img {
      height: 400px;
    }
  }

  @media (max-width: 768px) {
    .about-section {
      padding: 4rem 0;
    }

    .about-container {
      padding: 0 1rem;
    }

    .about-content {
      gap: 2.5rem;
    }

    .about-title {
      margin-bottom: 2rem;
    }

    .about-img {
      height: 350px;
    }
  }

  @media (max-width: 480px) {
    .about-section {
      padding: 3rem 0;
    }

    .about-content {
      gap: 2rem;
    }

    .about-title {
      margin-bottom: 1.5rem;
    }

    .about-paragraph {
      margin-bottom: 1.25rem;
    }

    .about-img {
      height: 300px;
    }
  }

  /* Multi-Day Tours Section */
  .multiday-tours-section {
    position: relative;
    background-image: url('/pictures/index/multiday-bg.webp');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    padding: 8rem 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .multiday-tours-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.6);
    z-index: 1;
  }

  .multiday-tours-container {
    position: relative;
    z-index: 2;
    max-width: 1000px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .multiday-tours-content {
    text-align: left;
  }

  .multiday-tours-title {
    font-size: clamp(2.5rem, 4.5vw, 4rem);
    font-weight: 300;
    letter-spacing: 1px;
    margin: 0 0 3rem 0;
    line-height: 1.2;
    color: white;
  }

  .multiday-tours-paragraph {
    font-size: clamp(1.1rem, 2.4vw, 1.4rem);
    font-weight: 300;
    line-height: 1.8;
    color: white;
    margin: 0 0 2rem 0;
    letter-spacing: 0.3px;
    opacity: 0.95;
  }

  .multiday-tours-paragraph:last-child {
    margin-bottom: 0;
  }

  /* Multi-Day Tours Section Mobile Responsive */
  @media (max-width: 768px) {
    .multiday-tours-section {
      background-attachment: scroll;
      padding: 4rem 0;
    }

    .multiday-tours-container {
      padding: 0 1rem;
    }

    .multiday-tours-title {
      margin-bottom: 2.5rem;
    }

    .multiday-tours-paragraph {
      margin-bottom: 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .multiday-tours-section {
      padding: 3rem 0;
    }

    .multiday-tours-title {
      margin-bottom: 2rem;
    }

    .multiday-tours-paragraph {
      margin-bottom: 1.25rem;
    }
  }

  /* Iceland for You Section */
  .iceland-for-you-section {
    background: white;
    padding: 8rem 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .iceland-for-you-container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .iceland-for-you-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .iceland-for-you-text {
    text-align: left;
  }

  .iceland-for-you-header {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }

  .iceland-for-you-logo-link {
    display: inline-block;
    flex-shrink: 0;
    transition: transform 0.3s ease;
  }

  .iceland-for-you-logo-link:hover {
    transform: scale(1.05);
  }

  .iceland-for-you-logo {
    height: 80px;
    width: auto;
    display: block;
  }

  .iceland-for-you-title {
    font-size: clamp(2.2rem, 4vw, 3.5rem);
    font-weight: 300;
    letter-spacing: 1px;
    margin: 0;
    line-height: 1.2;
    color: #1a1a1a;
  }

  .iceland-for-you-paragraph {
    font-size: clamp(1rem, 2.2vw, 1.2rem);
    font-weight: 300;
    line-height: 1.8;
    color: black;
    margin: 0 0 1.5rem 0;
    letter-spacing: 0.2px;
  }

  .iceland-for-you-paragraph:last-child {
    margin-bottom: 2rem;
  }

  .iceland-for-you-cta {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 10px 18px;
    background: transparent;
    color: #5b7297;
    text-decoration: none;
    border: 1px solid #5b7297;
    border-radius: 1px;
    font-weight: 300;
    font-size: clamp(0.75rem, 1.5vw, 0.9rem);
    transition: all 0.3s ease;
    flex-shrink: 0;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    position: relative;
    overflow: hidden;
  }

  .iceland-for-you-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: #5b7297;
    transform: translateY(-100%);
    transition: transform 0.8s ease;
    z-index: -1;
  }

  .iceland-for-you-cta:hover {
    color: #ffffff;
    transform: translateX(1px);
  }

  .iceland-for-you-cta:hover::before {
    transform: translateY(0);
  }

  .iceland-for-you-image {
    position: relative;
  }

  .iceland-for-you-img {
    width: 100%;
    height: 500px;
    object-fit: cover;
    object-position: center;
    border-radius: 2px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
  }

  .iceland-for-you-img:hover {
    transform: scale(1.02);
  }

  /* Iceland for You Section Mobile Responsive */
  @media (max-width: 968px) {
    .iceland-for-you-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .iceland-for-you-text {
      order: 1;
    }

    .iceland-for-you-image {
      order: 2;
    }

    .iceland-for-you-img {
      height: 400px;
    }

    .iceland-for-you-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .iceland-for-you-logo {
      height: 80px;
    }
  }

  @media (max-width: 768px) {
    .iceland-for-you-section {
      padding: 4rem 0;
    }

    .iceland-for-you-container {
      padding: 0 1rem;
    }

    .iceland-for-you-content {
      gap: 2.5rem;
    }

    .iceland-for-you-header {
      margin-bottom: 2rem;
    }

    .iceland-for-you-img {
      height: 350px;
    }
  }

  @media (max-width: 480px) {
    .iceland-for-you-section {
      padding: 3rem 0;
    }

    .iceland-for-you-content {
      gap: 2rem;
    }

    .iceland-for-you-header {
      margin-bottom: 1.5rem;
      gap: 0.75rem;
    }

    .iceland-for-you-logo {
      height: 70px;
    }

    .iceland-for-you-paragraph {
      margin-bottom: 1.25rem;
    }

    .iceland-for-you-img {
      height: 300px;
    }
  }

  /* Scroll Animation Classes */
  .fade-in {
    opacity: 0;
    transition: opacity 0.8s ease-out;
  }

  .fade-in.visible {
    opacity: 1;
  }

  .slide-up {
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .slide-up.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .slide-down {
    opacity: 0;
    transform: translateY(-50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .slide-down.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .slide-left {
    opacity: 0;
    transform: translateX(50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .slide-left.visible {
    opacity: 1;
    transform: translateX(0);
  }

  .slide-right {
    opacity: 0;
    transform: translateX(-50px);
    transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  }

  .slide-right.visible {
    opacity: 1;
    transform: translateX(0);
  }
</style>
	</head>
	<body>
		<HomeHeader frenchUrl="/fr/" currentLang="en" />
		<main>
			<section class="about-section fade-in">
				<div class="about-container">
					<div class="about-content">
						<div class="about-text slide-right">
							<h2 class="about-title">Lilja Tours, your local travel agency</h2>
							<p class="about-paragraph">
								Since our founding in 2021, Lilja Tours has been dedicated to creating unforgettable experiences across Iceland's breathtaking landscapes. We specialize in private, personalized tours that showcase both the iconic attractions and hidden gems that make Iceland truly magical.
							</p>
							<p class="about-paragraph">
								Our expert local guides bring years of experience and deep knowledge of Icelandic culture, geology, and history to every adventure. Whether you're chasing the Northern Lights, exploring volcanic landscapes, or discovering secluded waterfalls, we craft each journey to match your interests and pace.
							</p>
							<p class="about-paragraph">
								From intimate day tours to comprehensive multi-day expeditions, Lilja Tours offers luxury travel experiences that connect you authentically with Iceland's natural wonders while ensuring comfort, safety, and memories that will last a lifetime.
							</p>
						</div>
						<div class="about-image slide-left">
							<img src="/pictures/index/about.webp" alt="Lilja Tours guide with guests enjoying Iceland's stunning landscape" class="about-img" />
						</div>
					</div>
				</div>
			</section>
			
			<section class="itinerary-section fade-in">
				<div class="itinerary-container">
					<div class="itinerary-header slide-down">
						<h2 class="itinerary-title">Our best private tours</h2>
						<p class="itinerary-description">
							Discover Iceland through our carefully crafted private tours, each designed to showcase the country's most spectacular landscapes and hidden gems.
						</p>
					</div>
					
					<div class="itinerary-grid slide-up">
						{randomTours.map((tour) => (
							<ItineraryCard 
								image={tour.data.cardImg.srcPC || "/images/fallback-tour.jpg"}
								imageAlt={tour.data.cardImg.alt || tour.data.title}
								title={tour.data.title}
								duration={tour.data.duration || "Duration TBD"}
								description={tour.data.cardSentence || "Discover Iceland's beauty"}
								price={tour.data.price ? `From ${tour.data.price}` : "Price on request"}
								highlights={tour.data.highlights.filter(h => h.trim() !== "")}
								season={tour.data.season || "Year Round"}
							/>
						))}
					</div>
					
					<div class="itinerary-footer">
						<button class="itinerary-cta-button">Explore All Tours</button>
					</div>
				</div>
			</section>

			<section class="multiday-tours-section fade-in">
				<div class="multiday-tours-overlay"></div>
				<div class="multiday-tours-container">
					<div class="multiday-tours-content slide-up">
						<h2 class="multiday-tours-title">Personalized Multi-Day Tours</h2>
						<p class="multiday-tours-paragraph">
							Experience Iceland like never before with our fully customized multi-day private tours. We specialize in crafting comprehensive itineraries that go beyond the typical tourist path, creating personalized journeys that reflect your unique interests and travel style.
						</p>
						<p class="multiday-tours-paragraph">
							Our expert team works closely with you to design every aspect of your Icelandic adventure, from selecting the perfect accommodations and restaurants to timing each experience for optimal weather and lighting conditions. Whether you're seeking remote highland adventures, cultural immersion, or photography-focused expeditions, we create seamless multi-day experiences that maximize your time in Iceland.
						</p>
						<p class="multiday-tours-paragraph">
							With our comprehensive planning and local expertise, your multi-day tour becomes more than just a trip—it becomes a carefully orchestrated journey through Iceland's most spectacular landscapes, ensuring every moment is memorable and every detail is perfectly arranged.
						</p>
            <CTAButton></CTAButton>
					</div>
				</div>
			</section>

			<section class="iceland-for-you-section fade-in">
				<div class="iceland-for-you-container">
					<div class="iceland-for-you-content">
						<div class="iceland-for-you-image slide-left">
							<img src="/pictures/index/iceland-for-you.webp" alt="Iceland for You small group tour exploring Iceland's natural wonders" class="iceland-for-you-img" />
						</div>
						<div class="iceland-for-you-text slide-right">
							<div class="iceland-for-you-header">
								<a href="https://www.iceland-for-you.com" class="iceland-for-you-logo-link" target="_blank" rel="noopener noreferrer" aria-label="Visit Iceland for You website">
									<img src="/logos/iceland-for-you.png" alt="Iceland for You" class="iceland-for-you-logo" />
								</a>
								<h2 class="iceland-for-you-title">Small Group Adventures</h2>
							</div>
							<p class="iceland-for-you-paragraph">
								Meet Iceland for You, our sister brand dedicated to creating exceptional small group tours across Iceland's most spectacular destinations. While Lilja Tours focuses on private experiences, Iceland for You brings together like-minded travelers in intimate groups for shared adventures.
							</p>
							<p class="iceland-for-you-paragraph">
								Our small group tours offer the perfect balance of social connection and personalized attention, allowing you to share the wonder of Iceland's landscapes with fellow travelers while still receiving expert guidance from our knowledgeable local guides.
							</p>
							<p class="iceland-for-you-paragraph">
								From multi-day Ring Road expeditions to specialized Northern Lights tours, Iceland for You delivers authentic Icelandic experiences at exceptional value, combining the expertise you expect with the camaraderie that makes every journey memorable.
							</p>
							<a href="https://www.iceland-for-you.com" class="iceland-for-you-cta" target="_blank" rel="noopener noreferrer">
								VISIT ICELAND FOR YOU WEBSITE
							</a>
						</div>
					</div>
				</div>
			</section>
		</main>

		<script>
			// Intersection Observer for scroll animations
			function initScrollAnimations() {
				const observerOptions = {
					threshold: 0.1,
					rootMargin: '0px 0px -50px 0px'
				};

				const observer = new IntersectionObserver((entries) => {
					entries.forEach((entry) => {
						if (entry.isIntersecting) {
							entry.target.classList.add('visible');
						}
					});
				}, observerOptions);

				// Observe all elements with animation classes
				const animatedElements = document.querySelectorAll(
					'.fade-in, .slide-up, .slide-down, .slide-left, .slide-right'
				);

				animatedElements.forEach((element) => {
					observer.observe(element);
				});
			}

			// Initialize animations when DOM is loaded
			document.addEventListener('DOMContentLoaded', initScrollAnimations);
		</script>
	</body>


</html>
