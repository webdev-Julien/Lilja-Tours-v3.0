---
import "../../../src/styles/global.css"
import Topbar from "../../components/Topbar.astro"
import Footer from "../../components/Footer.astro"
import OptimizedImage from '../../components/OptimizedImage.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
	// Define all categories
	const categories = [
		{ name: 'Helicopter Tours', slug: 'helicopter-tours' },
		{ name: 'Hot Springs', slug: 'hot-springs' },
		{ name: 'Ice Caves', slug: 'ice-caves' },
		{ name: 'Lava Caves', slug: 'lava-caves' },
		{ name: 'Cultural Experiences', slug: 'cultural-experiences' },
		{ name: 'Small Group Tours', slug: 'small-group-tours' },
		{ name: 'ATV and Snowmobile', slug: 'atv-snowmobile' },
		{ name: 'Glacier Tours', slug: 'glacier-tours' },
		{ name: 'Whale Watching', slug: 'whale-watching' },
		{ name: 'Adventure', slug: 'adventure' }
	];

	const allExperiences = await getCollection('partner_experiences_EN');

	return categories.map((cat) => {
		// Filter experiences by category (case-insensitive match)
		const categoryExperiences = allExperiences.filter(exp =>
			exp.data.category.toLowerCase() === cat.name.toLowerCase()
		);

		return {
			params: { category: cat.slug },
			props: {
				categoryName: cat.name,
				experiences: categoryExperiences
			}
		};
	});
}

const { categoryName, experiences } = Astro.props;
const categorySlug = Astro.params.category;
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="description" content={`Discover ${categoryName} experiences in Iceland. Explore curated adventures and activities that showcase Iceland's unique landscapes and culture.`}>
		<link rel="alternate" hreflang="en" href={`https://www.lilja-tours.com/partner-experiences-iceland/${categorySlug}/`} />
		<link rel="alternate" hreflang="fr" href={`https://www.lilja-tours.com/fr/experiences-partenaires-islande/${categorySlug}/`} />
		<link rel="canonical" href={`https://www.lilja-tours.com/partner-experiences-iceland/${categorySlug}/`} />

		<meta property="og:title" content={`${categoryName} in Iceland | Lilja Tours`}>
		<meta property="og:type" content="website">
		<meta property="og:url" content={`https://www.lilja-tours.com/partner-experiences-iceland/${categorySlug}/`}>

		<link rel="icon" href="/favicon.ico" type="image/x-icon">
		<meta name="generator" content={Astro.generator} />
		<title>{categoryName} in Iceland | Lilja Tours</title>

		<!-- GDPR Cookie Consent -->
		<link rel="stylesheet" href="/css/gdpr-consent.css" />
		<script type="text/javascript" defer src="/js/gdpr-consent.js"></script>

		<!-- TrustIndex Rich Snippet -->
		<script type="text/javascript" defer async src="https://cdn.trustindex.io/assets/js/richsnippet.js?505b36156145g5ee"></script>

		<style is:inline>
			/* Animation classes */
			.fade-in {
				opacity: 0;
				transition: opacity 0.8s ease-out;
			}

			.fade-in.visible {
				opacity: 1;
			}

			.slide-up {
				opacity: 0;
				transform: translateY(50px);
				transition: opacity 0.8s ease-out, transform 0.8s ease-out;
			}

			.slide-up.visible {
				opacity: 1;
				transform: translateY(0);
			}

			/* Header background styles */
			.header-bg {
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				object-fit: cover;
				z-index: 0;
			}

			.header-overlay {
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: rgba(0, 0, 0, 0.5);
				z-index: 1;
			}

			.header-content {
				position: relative;
				z-index: 2;
			}

			/* Experience cards */
			.experience-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
				gap: 2rem;
			}

			.experience-card {
				position: relative;
				height: 400px;
				overflow: hidden;
				border-radius: 4px;
				transition: transform 0.3s ease, box-shadow 0.3s ease;
			}

			.experience-card:hover {
				transform: translateY(-4px);
				box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
			}

			.experience-card::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: linear-gradient(to bottom, rgba(0, 0, 0, 0.2) 0%, rgba(0, 0, 0, 0.6) 100%);
				z-index: 1;
			}

			.experience-card-bg {
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				object-fit: cover;
				transition: transform 0.3s ease;
			}

			.experience-card:hover .experience-card-bg {
				transform: scale(1.05);
			}

			.experience-card-content {
				position: relative;
				z-index: 2;
				height: 100%;
				display: flex;
				flex-direction: column;
				justify-content: flex-end;
				padding: 2rem;
				color: white;
			}

			/* Discover button (ghost style) */
			.discover-button::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: white;
				transform: translateY(-100%);
				transition: transform 0.6s ease;
				z-index: -1;
			}

			.discover-button:hover::before {
				transform: translateY(0);
			}

			.discover-button:hover {
				color: #5b7297;
			}

			/* Book now button (filled style) */
			.book-now-button::after {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: white;
				z-index: -2;
			}

			.book-now-button::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background: #5b7297;
				transform: translateY(-100%);
				transition: transform 0.6s ease;
				z-index: -1;
			}

			.book-now-button:hover::before {
				transform: translateY(0);
			}

			.book-now-button:hover {
				color: white;
			}
		</style>
	</head>
	<body>
		<Topbar frenchUrl={`/fr/experiences-partenaires-islande/${categorySlug}/`} currentLang="en" />

		<!-- Header Section -->
		<header class="relative py-32 overflow-hidden">
			<!-- Background Image -->
			<OptimizedImage
				src={`/pictures/experiences/${categorySlug}.webp`}
				alt={`${categoryName} in Iceland`}
				class="header-bg"
				sizes="100vw"
				loading="eager"
			/>

			<!-- Overlay -->
			<div class="header-overlay"></div>

			<!-- Content -->
			<div class="header-content max-w-7xl mx-auto px-8 text-center">
				<h1 class="text-5xl md:text-6xl lg:text-7xl font-light tracking-[2px] mb-8 leading-tight text-white slide-up">
					{categoryName}
				</h1>
				<p class="text-lg md:text-xl lg:text-2xl font-light leading-relaxed text-white opacity-95 tracking-wide max-w-4xl mx-auto slide-up">
					Explore our selection of {categoryName.toLowerCase()} in Iceland, carefully curated to provide exceptional experiences and unforgettable adventures.
				</p>
			</div>
		</header>

		<main>
			<!-- Experiences Section -->
			<section class="fade-in bg-white py-32">
				<div class="max-w-7xl w-full mx-auto px-8">
					{experiences.length > 0 ? (
						<div class="experience-grid slide-up">
							{experiences.map((experience) => (
								<div class="experience-card">
									<OptimizedImage
										src={experience.data.cardImg.srcPC}
										alt={experience.data.cardImg.alt}
										class="experience-card-bg"
										sizes="(max-width: 768px) 100vw, 33vw"
										loading="lazy"
									/>
									<div class="experience-card-content">
										<h3 class="text-2xl md:text-3xl font-light tracking-wide mb-3">{experience.data.title}</h3>
										<p class="text-sm md:text-base font-light mb-4 opacity-90">
											{experience.data.shortDescription}
										</p>
										<div class="flex flex-wrap items-center gap-4 text-sm mb-6">
											<span class="flex items-center gap-1.5">
												<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
												</svg>
												{experience.data.duration}
											</span>
											<span>•</span>
											<span class="flex items-center gap-1.5">
												<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
												</svg>
												{experience.data.location}
											</span>
											<span>•</span>
											<span>From {experience.data.price.toLocaleString()} ISK</span>
										</div>
										<div class="flex gap-3 mt-auto">
											<a
												href={experience.data.url}
												class="discover-button flex-1 inline-flex items-center justify-center gap-2 py-2 px-4 bg-transparent text-white border border-white rounded-sm font-light text-xs tracking-wide uppercase relative overflow-hidden"
											>
												Discover
											</a>
											<a
												href={experience.data.bookingLink}
												class="book-now-button flex-1 inline-flex items-center justify-center gap-2 py-2 px-4 bg-transparent text-gray-900 border border-white rounded-sm font-light text-xs tracking-wide uppercase relative overflow-hidden"
											>
												Book Now
											</a>
										</div>
									</div>
								</div>
							))}
						</div>
					) : (
						<div class="text-center py-16">
							<p class="text-xl text-gray-600 mb-8">
								No experiences available in this category yet. Please check back soon!
							</p>
							<a
								href="/partner-experiences-iceland/"
								class="inline-block bg-[var(--secondary)] text-white px-8 py-3 rounded-sm font-medium tracking-wide hover:bg-[#5b7297] transition-all duration-300"
							>
								Browse All Experiences
							</a>
						</div>
					)}
				</div>
			</section>

			{/* Photo Gallery Link - only for Helicopter Tours */}
			{categorySlug === 'helicopter-tours' && (
				<section class="fade-in bg-gray-50 py-16">
					<div class="max-w-7xl w-full mx-auto px-8 text-center">
						<h2 class="text-3xl md:text-4xl font-light tracking-wide mb-6 text-black">See Iceland from Above</h2>
						<p class="text-lg md:text-xl font-light leading-relaxed text-gray-700 max-w-3xl mx-auto mb-8">
							Our guides capture stunning aerial photography during private helicopter tours. Browse our photo gallery showcasing glaciers, volcanoes, and highlands from above.
						</p>
						<a
							href="/blog/best-pictures-helicopter-private-tours/"
							class="inline-block bg-[#5b7297] text-white px-8 py-3 rounded-sm font-light tracking-wide hover:bg-[#4a5f7a] transition-all duration-300"
						>
							View Photo Gallery
						</a>
					</div>
				</section>
			)}
		</main>

		<Footer currentLang="en" />

		<script>
			// Intersection Observer for scroll animations
			function initScrollAnimations() {
				const observerOptions = {
					threshold: 0.1,
					rootMargin: '0px 0px -50px 0px'
				};

				const observer = new IntersectionObserver((entries) => {
					entries.forEach((entry) => {
						if (entry.isIntersecting) {
							entry.target.classList.add('visible');
						}
					});
				}, observerOptions);

				const animatedElements = document.querySelectorAll('.fade-in, .slide-up');
				animatedElements.forEach((element) => {
					observer.observe(element);
				});
			}

			document.addEventListener('DOMContentLoaded', initScrollAnimations);
		</script>
	</body>
</html>
